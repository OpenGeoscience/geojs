language: node_js
sudo: False
node_js:
    - "0.12"

cache:
    directories:
        - node_modules
        - $HOME/cache

before_install:
    - CACHE="${HOME}/cache" CMAKE_VERSION=3.5.0 CMAKE_SHORT_VERSION=3.5 source ./scripts/install_cmake.sh
    - npm prune
    - npm install

script:
    - npm run build
    - ./node_modules/.bin/grunt docs
    - ./node_modules/.bin/grunt copy:jqueryui
    - mkdir _build
    - ctest -S cmake/travis_build.cmake -VV

after_success:
  - npm run codecov
